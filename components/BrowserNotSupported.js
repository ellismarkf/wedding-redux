import React from 'react'

const BrowserNotSupported = () => (
	<div>
		<h1>Sorry, the browser you're using isn't supported.  Try opening the link in Chrome or Firefox!</h1>
	</div>
)

export default BrowserNotSupported